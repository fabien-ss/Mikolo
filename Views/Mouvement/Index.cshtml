@model IEnumerable<mikolo.Mouvement>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateMouvement)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entree)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sortie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdLaptopNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateMouvement)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Entree)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sortie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdLaptopNavigation.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
